const initInstanceProps = (instance) => {
  const instanceProps = {
    rowMetaDataMap: new Map(),
    columnMetaDataMap: new Map(),
  };
  //check for instance refresh after indices for change in height of a particular row or column
  return instanceProps;
};
